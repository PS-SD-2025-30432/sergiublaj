export enum ROUTES {
  ABOUT = 'about',
  ALL = '**',
  AUTH = 'auth',
  CHEFS = 'chefs',
  EMPTY = '',
  FORBIDDEN = 'forbidden',
  ID = ':id',
  LOGIN = 'login',
  NOT_FOUND = 'not-found',
  PROFILE = 'profile',
  USERS = 'users'
}
